import { Question, Answer, answerLabels } from '../data/questions';
export function QuestionCard({question,value,onChange,onInfo}:{question:Question;value:Answer|undefined;onChange:(a:Answer)=>void;onInfo:(q:Question)=>void}){return <section className="question-row"><div className="question-name">{question.title}<button onClick={()=>onInfo(question)} aria-label={`查看${question.title}解释`}>?</button></div><div className="answers">{answerLabels.map(([id,label])=><button key={id} className={`${value===id?'chosen':''} ${id==='avoid'?'avoid':''}`} onClick={()=>onChange(id)}>{label}</button>)}</div></section>}
